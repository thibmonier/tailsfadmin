<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />

        <!-- Panther T-TECH-02 : driver headless Chromium. -->
        <!-- Prérequis : Chromium installe + vendor/bin/bdi detect drivers -->
        <!-- Lancer avec : composer test:e2e -->
        <env name="PANTHER_NO_SANDBOX" value="1" />
        <env name="PANTHER_CHROME_BINARY" value="/Applications/Chromium.app/Contents/MacOS/Chromium" />
        <env name="PANTHER_CHROME_DRIVER_BINARY" value="drivers/chromedriver" />
    </php>

    <!-- Suite par defaut (composer test) : WebTestCase uniquement, sans E2E. -->
    <testsuites>
        <testsuite name="Project Test Suite">
            <directory>tests</directory>
            <exclude>tests/E2E</exclude>
        </testsuite>

        <!-- Suite E2E Panther : composer test:e2e (ou phpunit avec l'option testsuite e2e) -->
        <testsuite name="e2e">
            <directory>tests/E2E</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <deprecationTrigger>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
        <bootstrap class="Symfony\Component\Panther\ServerExtension" />
    </extensions>
</phpunit>
